# Number-Rush
Unity mini-game that integrates an SMTP email notification system. When a relevant game event occurs, the system dynamically generates and sends a real email using the provided SMTP code. The UI allows entering a destination email, triggering the event, and viewing the sending status.
